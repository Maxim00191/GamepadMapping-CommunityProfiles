# Bundle submission API (schemaVersion + files[]). Deploy with: wrangler deploy
# Secrets (wrangler secret put): GH_TOKEN, UPLOAD_API_KEY
# Optional bindings: LIMIT_KV; optional vars: DAILY_UPLOAD_LIMIT
# Client: Gamepad Mapping app (CommunityTemplateWorkerSubmissionPayload over HTTPS POST + Bearer UPLOAD_API_KEY)
name = "gamepadmapping-community-upload"
main = "src/index.ts"
compatibility_date = "2026-01-01"

[vars]
REPO_OWNER = "Maxim00191"
REPO_NAME = "GamepadMapping-CommunityProfiles"
BASE_BRANCH = "main"